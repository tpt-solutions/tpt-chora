[package]
name = "tpt-chora-fallback"
description = "tpt-chora Three-Tier Hardware Fallback: software rasterization, headless output, dynamic fidelity"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["fallback", "software-rendering", "headless", "fidelity", "rasterization"]
categories = ["rendering", "hardware-support"]

[dependencies]
tpt-chora-render = { workspace = true }
wgpu = "0.19"
thiserror = "1.0"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
png = "0.17"
