# tpt-chora-all-subsystems

Internal integration demo binary (`all_subsystems_demo`) exercising every [tpt-chora](https://github.com/tpt-solutions/tpt-chora) subsystem end-to-end. This crate is not published to crates.io (`publish = false`) — it exists to validate the workspace as a whole, not for external consumption.
